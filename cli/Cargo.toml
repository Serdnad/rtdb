[package]
name = "rtdb_cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "rtdb_cli"
path = "src/main.rs"

[dependencies]
rtdb_client = { path = "../client" }

rustyline = "10.0.0"
tokio = { version = "1.21.1", features = ["full"] } # for running tests with async
